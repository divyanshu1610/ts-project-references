{
  "extends": "../tsconfig-base.json",
  "compilerOptions": {
    "outDir": "../../lib/coffee",
    "tsBuildInfoFile": "../../.ts/coffee.tsbuildinfo"
  },
  "references": [
    {
      "path": "../common" // because we are using functions of "common" project
    }
  ]
}
